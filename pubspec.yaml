name: ewaste_locator
description: A modern e-waste locator app

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_maps_flutter: ^2.5.3
  geolocator: ^10.1.0
  flutter_animate: ^4.5.0
  provider: ^6.1.1
  http: ^1.2.0
  lottie: ^2.7.0
  flutter_svg: ^2.0.9
  cached_network_image: ^3.3.1
  shared_preferences: ^2.2.2
  url_launcher: ^6.2.5

flutter:
  assets:
    - assets/map_style.json
